{
  "name": "agent-flow-kit",
  "version": "0.1.0",
  "description": "Project-agnostic AI workflow kit. Single entry, AI-driven phase progression. Works with Claude and Codex.",
  "scripts": {
    "parity:check": "node scripts/check-agent-flow-parity.mjs",
    "skills:validate": "node scripts/validate-skills.mjs",
    "test": "PYTHONPATH=src python3 -m unittest tests.test_cli -q && PYTHONPATH=src python3 -m pytest -q tests/test_custom_skill_install.py tests/test_skill_runtime_parity.py tests/test_architecture_lint.py && node --test tests/test_skill_source_runtime.mjs && node scripts/check-agent-flow-parity.mjs"
  },
  "bin": {
    "agent-flow": "bin/agent-flow-kit.mjs",
    "agent-flow-install": "bin/agent-flow-install.mjs",
    "agent-flow-kit": "bin/agent-flow-kit.mjs"
  },
  "files": [
    "bin",
    "lib",
    "workflows",
    "profiles",
    "templates",
    "bootstrap",
    "skills",
    "scripts",
    ".Codex",
    ".claude",
    "src",
    "pyproject.toml",
    "README.md"
  ],
  "license": "MIT",
  "type": "module"
}
