{
  "kind": "addition",
  "subject": "candidate line F6: the shell must render from data a plain request can obtain. Removed after #247: once cold-shell accepted the wording reviewers use without the line, both arms caught the empty shell 24/24. Re-adding it must surface a miss the guide otherwise loses.",
  "baseline": "skills/react-development-guide/SKILL.md",
  "variant": {
    "insertAfter": 25,
    "expectAnchor": "Server HTML and the first client hydration render must use the same snapshot",
    "text": [
      "- Render the shell from data a plain request can obtain. A first-visit or crawler request that misses build-warmed state must still receive the shell."
    ]
  },
  "fixtures": [{ "name": "target", "file": "fixture.diff", "expect": "request-changes" }],
  "patterns": {
    "cold-shell": "crawler|크롤러|\\bbot\\b|봇|prerender|프리렌더|module[- ](state|scope|level)|module (변수|단위)|모듈 (상태|스코프|수준|전역|변수|단위)|프로세스 (전역|로컬)|process-local|cold start|콜드|empty (shell|document|page)|빈 (문서|셸|화면|페이지)|null을 반환|returns? null|`null`",
    "cache-staleness": "revalidate|캐시 무효화|stale data|오래된 데이터"
  },
  "target": "cold-shell"
}
