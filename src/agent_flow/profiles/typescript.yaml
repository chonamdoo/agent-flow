id: typescript
branching:
  strategy: trunk
  base: main
  integration: main
  worktree: required
  naming:
    prefix: "feat/"
    slug_style: kebab-case
    slug_source: feature title
    max_slug_length: 60
gates:
  - id: architecture-lint
    command: ["agent-flow", "architecture-lint", "--profile", "typescript"]
    required: true
    phase: pre-commit
  - id: typecheck
    command: ["npx", "tsc", "--noEmit"]
    required: true
    phase: pre-commit
  - id: test
    command: ["npm", "test"]
    required: true
    phase: pre-commit
  - id: lint
    command: ["npm", "run", "lint"]
    required: false
    phase: pre-commit
  - id: build
    command: ["npm", "run", "build"]
    required: false
    phase: pre-push
review_angles:
  - id: architecture-design
    prompt: templates/_shared/review/architecture-design.md
  - id: types
    prompt: templates/_shared/review/types.md
  - id: io-safety
    prompt: templates/_shared/review/io-safety.md
skills:
  install:
    - typescript-development-guide
  required_review:
    - group: profile
      skills: [typescript-development-guide]
      when: TypeScript files change
      missing: "missing local profile: <skill>"
artifacts:
  prd: optional
  adr: optional
  slice_plan: required
vocabulary:
  prd: spec
  adr: decision-log
  lore: notes
commit_convention:
  style: conventional
  co_author: skip
pr:
  target_branch: main
  merge_strategy: squash
