# Claude Task Stage: {{stage_id}}

Use a task/subagent for role `{{role}}`.

Workflow: {{workflow_id}}
Run: {{run_id}}
Replica: {{replica}}/{{replicas}}

Task:
{{task}}

Return concise findings, changes, verification, and blockers.
