# Codex Subagent Stage: {{stage_id}}

Spawn a subagent for role `{{role}}`.

Workflow: {{workflow_id}}
Run: {{run_id}}
Architecture: {{architecture}}
Replica: {{replica}}/{{replicas}}

Task:
{{task}}

Architecture guidance:
{{architecture_guidance}}

Write the result as an artifact summary.
